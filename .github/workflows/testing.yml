name: Testing

on:
  push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Update gcc
        run: sudo apt-get Update
        run: sudo apt --only-upgrade install gcc
      - run: g++ -std=c++17 -lstdc++fs *.cpp
      - run: ./testing.sh
      - run: diff output.txt exp_output.txt
